# 인수인계 문서 — PCB 검사 시뮬레이터 / 스텝모터 제어

> **문서 버전: v1.3** · 최종 갱신 2026-08-26
> **사용법**: 새 채팅 첫 메시지에 이 문서 전체를 붙여넣으면 이어서 작업 가능합니다.
> 담당: 인시스 AI혁신 연구개발실 · ys.kim@in-sys.co.kr

---

## 1. 작업 규칙 (반드시 지킬 것)

| 항목 | 내용 |
|---|---|
| 언어 | 모든 설명·안내는 한국어, 결론 먼저 |
| 버전 | 수정 시 **버전 필수 갱신** (코드 내 값 + 화면 표시 동시) |
| 수정 범위 | **지시한 내용만** 수정, 기존 동작 코드 임의 변경 금지 |
| 검증 | 전달 전 **오류 자체 검증** 후 전달 |
| 파일명 | 고정(덮어쓰기) — 링크 불변, 내용만 최신 |
| 폰트 | HTML은 Noto Sans KR 단일, 최소 10px |
| 색상 | 비즈니스 색상(네이비/그레이), 저대비 금지 |
| 저장소 | GitHub **Insys-HQ/Insys-review** (main) |
| 업로드 | 코드 완성 시 **묻지 말고 바로 업로드** + 링크 첨부 |

### GitHub 업로드 방법
- Claude가 직접 `git push` 수행. **토큰이 그 대화에 없으면 1회만 요청**, 이후 같은 대화에서 재사용.
- 토큰은 **프로젝트 지침에 상주**시켜 두었음. 단 지침 변경은 **저장 이후 새로 시작하는 대화부터** 적용되므로, 지침 수정 직후 대화에서는 반영되지 않을 수 있음.
- 토큰 형식: `ghp_...` (classic PAT, `repo` 권한)
  - ⚠ classic PAT는 계정의 **모든 리포지토리에 쓰기 권한**이 열림. `Insys-review` 하나만 묶은 **fine-grained PAT**(Contents = Read and write, 만료 90일)로 교체 권장.
- 명령 예시:
```bash
git clone --depth 1 "https://x-access-token:<TOKEN>@github.com/Insys-HQ/Insys-review.git" up
cd up && cp /mnt/user-data/outputs/파일명 .
git -c user.name="Insys AI Lab" -c user.email="ys.kim@in-sys.co.kr" add 파일명
git -c user.name="Insys AI Lab" -c user.email="ys.kim@in-sys.co.kr" commit -m "메시지"
git push origin HEAD
```
- 업로드 후 **항상 링크 첨부**:
  - 소스: `https://github.com/Insys-HQ/Insys-review/blob/main/<파일명>`
  - 실행: `https://insys-hq.github.io/Insys-review/<파일명>`
- 반영에 1~2분 소요. 구버전이 보이면 `Ctrl+F5`.

### 지시 범위 원칙 (2026-08-24 추가) ★중요
- **요청하지 않은 구조 변경 금지.** 요청을 달성하기 위해 구조 변경이 필요해 보이면 **먼저 안을 제시하고 확인**받을 것.
- 실패 사례: "축이 기둥 안쪽에서 이동해야 한다"는 요청에 기둥을 2주→4주로 임의 확장 → 반려됨.
- 반대로, **명시적으로 제한하지 않은 값**(예: 브릿지 높이)은 요청 달성에 필요하면 조정 가능. 단 가정 내용은 반드시 명시.
- **지시가 이전 사양과 충돌하면** 최신 지시를 우선 적용하되, 충돌 사실과 되돌리는 방법을 반드시 함께 보고할 것. (예: "150mm 돌출" → "모든 구조물 프레임 내부" → "좌우 돌출"로 3회 번복된 이력 있음)
- **방향 용어 확인**: "돌출"은 이송축(좌우) 기준. 전면(Y) 돌출과 혼동하지 말 것.

### 기존 공유분 취급 원칙 (2026-08-24 추가)
- 이미 **외부 공유된 HTML은 소급 수정하지 않는 것이 기본**. 신규 작업분에만 새 규칙 적용.
- 단, 사용자가 명시적으로 지시하면 기존 파일도 수정 (컨베이어 라운드 건이 이 케이스).

---

## 2. 파일 현황 (8개)

| 파일 | 버전 | 내용 | 하드웨어 |
|---|---|---|---|
| `3Motor.html` | v5.5 | 스텝모터 3축 웹 제어 대시보드 | **실제 연결** (아두이노) |
| `pnp_flow.html` | v3.6 | PCB 픽앤플레이스 반송 시뮬레이터 | 미연결(시뮬레이션) |
| `vision.html` | **v1.7** | 비전 검사 — **6축 다관절 로봇** | 미연결 |
| `vision3.html` | **v3.7** | 비전 검사 — **갠트리 5축 스테이지** | 미연결 |
| `Vision-H1.html` | **v1.3** | 비전 검사 — **헥사포드 6축** | 미연결 |
| `vision-jig.html` | **v1.4** | **안착지그 2대 순환식** (리프터 승강) | 미연결 |
| `jig2.html` | **v1.1** | **지그 7EA 벨트 고정** · 작업대 매립형 | 미연결 |
| `frame.html` | **v1.8** | **실물 검사기** 500×500×1500 프레임 ★최신 | 미연결 |

실행 링크: `https://insys-hq.github.io/Insys-review/<파일명>`

---

## 3. 파일별 상세

### 3-1. `3Motor.html` (v5.5) — 스텝모터 웹 제어
- **Web Serial API**로 아두이노와 실제 통신 (Chrome/Edge 전용, 9600bps)
- 28BYJ-48 스텝모터 3개 + ULN2003 드라이버
- 기능: 연속 회전(CW/정지/CCW), 속도 슬라이더(RPM 표시), 지정값 구동(스텝/각도), 원점 저장·복귀, **PLC 스타일 시퀀스 프로그램**(순차왕복/순차이동/동시지연, 반복 설정, PUSH 스위치, 타임라인), 통신 로그
- **레이아웃**: 1화면 대시보드. 헤더 우측에 포트연결+상태, 좌측 3열 모터 제어 + 하단 통신 로그, 우측 실시간 모니터링 + 시퀀스
- **타깃 화면**: 1920×1080 **125% 확대** → 실효 CSS 뷰포트 **1536×864** 기준으로 맞춤
- 대응 펌웨어: `step_motor.ino v3.1` (명령 `"모터번호:명령"`, 상태 브로드캐스트 `STAT:...`)

### 3-2. `pnp_flow.html` (v3.6) — 픽앤플레이스 반송 시뮬레이터
- 6축 스윙암 로봇이 PCB를 **집어서** 이동시키는 방식
- 배치: 컨베이어 위 1(투입)·2(픽업)·4(OK배출), 위쪽에 3(검사지그)·5(NG배출), 로봇 베이스는 컨베이어 아래 중앙
- **파이프라인 흐름**: 3번 안착 시 새 PCB가 1번 등장 → 검사 중 1→2 이동 대기 → 배출 시 이미 2번에 대기
- 컨베이어는 **PCB 이송 시에만** 동일 속도로 회전, OK품은 다음 이송 때 우측으로 배출
- 텔레스코픽 팔(회전+신축)
- ※ 컨베이어 라운드 마감 **미적용** (필요 시 4-1 코드 이식)

### 3-3. `vision.html` (v1.7) — 6축 로봇 비전 검사
- 로봇이 PCB를 **집지 않고**, 컨베이어 위 PCB를 그대로 검사
- 6축 로봇 끝단에 Cognex 카메라 + 돔 조명
- 기울어진 PCB에 카메라를 **직각 정렬**, **초점거리 300mm 유지**
- NG 시 알람+검사 중단 → 작업자 제거 후 [검사 계속]로 재개
- 3D 축측투영, 시점 슬라이더 0°(정면)~90°(측면)
- **v1.7**: 컨베이어 양끝 라운드 마감 적용 + 기존 **원형 풀리(r=15) 2개 제거**

### 3-4. `vision3.html` (v3.7) — 갠트리 5축 스테이지
- **뒤쪽 기둥 2주**(`BRIDGE_X±GXS`, `GY_B`) + 그 사이를 잇는 **X축 레일 1줄**
- X축 캐리지가 **기둥 2주 안쪽에서만** 이동 → 그 위에 **Y축 브릿지가 앞으로 캔틸레버**
- 앞 기둥 없음 → 카메라·돔조명 시야 간섭 없음
- 5축: X, Y, Z, θ, φ — 상단에 mm/도 실시간 표시(구동 중 노랑, 정렬 완료 초록)
- **위치확인 카메라**(수직 하향, `ax+24`)로 PCB 중심·기울기 측정 → 축 구동
- 주요 상수: `XR=34`(X 스트로크 ±113mm, 실측 필요치 ±87mm), `GXS=58`(기둥 간격 ±), `XZ=13`
- **v3.6→v3.7 이력**: v3.6에서 4주 갠트리로 잘못 확장 → v3.7에서 앞 2주 삭제, 뒤 2주만 유지
- **v3.6**: X축 레일 480mm→387mm 단축, 축이 기둥 안쪽에서 이동하도록 변경
- **v3.5**: 컨베이어 양끝 라운드 마감 적용. X 좌표 표시는 `BELT_X0` 기준이라 측정값 변동 없음

### 3-5. `Vision-H1.html` (v1.3) — 헥사포드 6축 ★최신 컨셉
- **N형 브릿지 600mm** + 빔 하단 밀착 상부 브라켓 + **역설치 헥사포드**(베이스 위 / 무빙 플랫폼 아래, HEGOA 형태를 상하 반전)
- 브릿지 높이는 **제한 조건 아님** — 스트럿 가동 가시성 확보를 위해 400→600mm 상향(2026-08-24 확정)
- 6개 액추에이터 스트럿이 실제 길이 변화하며 **X·Y·Z·Rx·Ry** 구동
- 무빙 플랫폼 하부 브라켓에 **비전 카메라 + 돔 조명**
- **비전 카메라 엣지 신호**로 PCB 외곽 검출 → 중심 좌표·기울기 산출(녹색 엣지/십자 마커) → 헥사포드가 항상 **PCB 중심 + 직각 + 초점 300mm** 유지
- 흐름: 투입 → 센서 → 조명 ON → 엣지 검출 → X·Y 정렬 → Rx·Ry 직각 → Z 초점 → 스캔 → 판정/MES → (NG 알람·재개) → 원점 복귀 → OK 배출
- 높이 구성: `BRIDGE_Z` → `BRK_Z=BRIDGE_Z-8`(브라켓) → `BASE_Z=BRK_Z-3`(헥사포드 베이스) → 스트럿 → 무빙 플랫폼 → 카메라
- `HOME.pz=BASE_Z-52`(스트럿 수축 원점), 작업 시 `pz≈157` → 스트럿 실측 **174~279mm**
- **v1.3**: 브릿지 600mm, 헥사포드를 빔 하단에 매다는 형태로 재작도, 스트럿 신축 가시화
- **v1.2**: 컨베이어 양끝 라운드 마감 최초 적용

---

### 3-6. `vision-jig.html` (v1.4) — 안착지그 2대 순환식
- **안착지그**: 베이스 + 대각 위치결정 핀 2점 + 스프링 래치 2점. PCB를 올리면 **딸깍** 고정 → 좌표·수평 0.0° 보장
- **순환 구조**: 상부 이송면 / 하부 리턴면 2단. 양끝 데크를 파낸 **승강 개구부** 2곳에 리프터 A(상승)·B(하강)
- **래치 해제**: 컨베이어 앞 가장자리 램프형 **해제 캠**. 지그 롤러가 닿는 순간부터 80mm 구간에 걸쳐 점진 개방
- **PCB 분리**: 리프터 B 진입 시 지그는 하강, 고정 **스트리퍼 레일**이 PCB를 잡아 분리 → 푸셔 → 배출 슈트
- **비전**: 구동축 없는 고정 헤드 (지그가 수평·정위치 보장하므로 정렬축 불필요)
- **파이프라인 (v1.2~v1.4)**: 지그 2대 교대 운전
  - `feed` — 검사 지그가 검사위치로, 대기 지그는 하부 리턴
  - `standby` — 검사 중에 대기 지그가 **안착위치로 상승·적재**
  - `release` — 검사 지그 배출 이동과 **동시에** 대기 지그가 검사위치로 진입 (안전간격 76unit 상시 감시)
  - `discharge` — PCB 분리 즉시 빈 지그가 하부 리턴 개시
- ⚠ **간섭 주의**: 빈 지그를 분리 직후 안착위치까지 *상승*시키면 대기 지그와 동일 좌표에서 겹침. 하부 리턴까지만 즉시 수행하고 상승은 앞 지그 출발 후

### 3-7. `jig2.html` (v1.1) — 지그 7EA 벨트 고정 · 작업대 매립
- 리프터 승강식이 아니라 **지그를 벨트에 브래킷 체결**하고 풀리를 돌아 순환
- 풀리 반경 **120mm** (지그 반길이 80mm의 1.5배) → 벨트 상하면 간격 240mm
- 벨트 전장 2,800mm / 지그 피치 400mm / **7EA**
- 스테이션 3곳(안착·검사·배출)이 **정확히 1피치 간격** → 1회 인덱스마다 3곳이 동시에 새 지그로 채워짐 (대기 0)
- 택트: 검사 1.0초 → 2.4초(1,500개/h) / 검사 3.0초 → 4.4초(820개/h)
- **작업대 매립**: 상판이 벨트 상면과 동일 높이. 중앙에 직사각 개구부를 뚫어 컨베이어를 넣고, **상부 주행 벨트만 노출**
- 컨베이어 측판 턱을 벨트 상면과 flush 처리 (외경 R+12 → R)

### 3-8. `frame.html` (v1.8) — 실물 검사기 ★최신
- **프레임 500(L)×500(W)×1500(H)**, 30×30 알루미늄 프로파일
- 컨베이어 **H700**, 좌우(이송축) **150mm씩 돌출** — 전면 돌출 아님
- 폭 배분: 전면커버 **50** + 벨트 **200** + 후면커버 **250** = 500 (여유 0)
- 커버는 **프레임 내부에만**. 돌출부는 커버 밖으로 나옴
- 하부: **아이보리** 캐비닛 + **양문형 도어**(세로 바 손잡이·힌지 2점·잠금 실린더) + 환기 그릴 + `INSYS` 로고 + HMI 조작반
- 상부: 아크릴 **안전커버** 3면 (천장 없음). NG 시 인터록 해제로 투명도 변화
- **지그 7EA** 벨트 고정 (풀리 R50, 피치 245mm)
- 비전 헤드는 상부 프레임에 브래킷 고정. 헤드 200mm · WD 250mm
- 커버 체크박스로 안전커버 ON/OFF 가능

---

## 4. 공통 기술 사양 (시뮬레이터 3종)

### 좌표/투영
- 3D 축측 투영: X=컨베이어 방향, Y=깊이, Z=높이
- 시점: yaw 0~90° 슬라이더 + 정면/45°/측면 버튼, pitch 22° 고정
- 스케일: **90 unit ≙ 300mm** (1 unit ≈ 3.333mm)
- 컨베이어 기준값: `BELT_X0=133, BELT_X1=567, BELT_Y0=0, BELT_Y1=70, BELT_Z=60, BELT_TH=18, RAIL_W=9, RAIL_Z=64`

### 4-1. 컨베이어 양끝 라운드 마감 ★신규 표준
> 원형 풀리를 그리는 방식이 아니라 **벨트 끝 반원 노즈** 마감. 신규 컨베이어 작업 시 기본 적용.

```javascript
const NR=(RAIL_Z-(BELT_Z-BELT_TH))/2, NZC=(RAIL_Z+(BELT_Z-BELT_TH))/2; // 프레임 노즈 r≈11u(37mm)
const NXL=BELT_X0+NR, NXR=BELT_X1-NR;                                   // 노즈 중심 X
const BWR=BELT_TH/2, BWZ=BELT_Z-BWR;                                    // 벨트 랩 r=9u (프레임보다 2u 안쪽)

function framePath(Y){ const N=14, ps=[];
  for(let i=0;i<=N;i++){ const a=(-90+180*i/N)*Math.PI/180; ps.push(P(NXR+NR*Math.cos(a),Y,NZC+NR*Math.sin(a))); }
  for(let i=0;i<=N;i++){ const a=( 90+180*i/N)*Math.PI/180; ps.push(P(NXL+NR*Math.cos(a),Y,NZC+NR*Math.sin(a))); }
  return ps.map(pt).join(' '); }

function wrapFace(cx,rr,zc,dir,y1,y2,fill,stroke){ const N=12, u=[], v=[];
  for(let i=0;i<=N;i++){ const a=(90-180*i/N)*Math.PI/180, X=cx+dir*rr*Math.cos(a), Z=zc+rr*Math.sin(a);
    u.push(P(X,y1,Z)); v.push(P(X,y2,Z)); }
  return `<polygon points="${u.concat(v.reverse()).map(pt).join(' ')}" fill="${fill}" stroke="${stroke}" stroke-width="1"/>`; }
```

**드로우 순서 (가림 순서 정확도에 필수)**
1. 프레임 측면 `framePath(BELT_Y0)` → `framePath(BELT_Y1)`
2. **후면 레일 노즈 랩** `wrapFace(..., yB, BELT_Y1)` ← 벨트보다 먼저
3. 벨트 상면 (`NXL~NXR`로 축소)
4. 벨트 노즈 랩 좌/우 (`BWR`, `BWZ`)
5. 트레드 선 (`for(let rx=NXL+off-sp; rx<NXR; rx+=sp)`)
6. 레일 상면 전/후 (`NXL~NXR`로 축소)
7. **전면 레일 노즈 랩** `wrapFace(..., BELT_Y0, yA)`

**주의**
- 3D 아크를 샘플링해 `P()`로 투영 → 시점 0~90° 전 구간 자동 대응
- 벨트 상면 위에 얹은 하이라이트/광택 스트립도 `NXL~NXR`로 축소해야 노즈를 침범하지 않음
- 벨트 색상이 파일마다 다름: `vision.html`은 검정(`#1B1F24`), 나머지는 그레이(`#7E8792`) → 랩 스트로크도 각 파일 색에 맞출 것

### 4-2. 벨트 루프 + 지그 고정 (jig2 / frame 공통) ★표준
```javascript
const R=15, ZC=CONV_Z-R, LO_Z=ZC-R;              // 풀리 반경 / 벨트 중심 / 하부 주행면
const PX_L=CX0+R, PX_R=CX1-R, LSTR=PX_R-PX_L;
const NJIG=7, PATH=2*LSTR+2*Math.PI*R, PITCH=PATH/NJIG;
const A1=LSTR, A2=LSTR+Math.PI*R, A3=2*LSTR+Math.PI*R;
function beltPos(s){                              // 벨트 파라미터 s → 위치 + 접선/법선
  s=((s%PATH)+PATH)%PATH;
  if(s<A1) return {X:PX_L+s, Z:CONV_Z, nx:0, nz:1, tx:1, tz:0};
  if(s<A2){ const th=Math.PI/2-(s-A1)/R;
    return {X:PX_R+R*Math.cos(th), Z:ZC+R*Math.sin(th), nx:Math.cos(th), nz:Math.sin(th), tx:Math.sin(th), tz:-Math.cos(th)}; }
  if(s<A3) return {X:PX_R-(s-A2), Z:LO_Z, nx:0, nz:-1, tx:-1, tz:0};
  const th=-Math.PI/2-(s-A3)/R;
  return {X:PX_L+R*Math.cos(th), Z:ZC+R*Math.sin(th), nx:Math.cos(th), nz:Math.sin(th), tx:Math.sin(th), tz:-Math.cos(th)};
}
```
**지그 작도**: 지그 로컬 좌표를 접선(`t`)·법선(`n`)으로 변환해 벨트 자세를 그대로 따라가게 함
```javascript
const JP=(du,dv,dn)=>P(p.X+p.tx*du+p.nx*dn, CYC+dv, p.Z+p.tz*du+p.nz*dn);
```
**주의**
- 풀리 반경은 **지그 반길이의 1.5배 이상**이어야 랩 구간이 자연스러움
- 상부(`nz>0.9`) / 하부·랩 구간 지그를 나눠 도시. 하부 지그는 면 순서를 뒤집을 것
- 측판을 채운 스타디움으로 그리면 하부 지그가 가려짐 → **링(밴드)** 형태로 작도

### 4-3. 프레임 모서리 맞물림 (frame) ★표준
부재 **중심선**을 외곽선에 맞추면 절반씩 바깥으로 돌출됨. **외곽면**을 기준으로 작도할 것.
```javascript
const pxs=sx=>sx<0?FX0:FX1-PROF, pys=sy=>sy<0?FY0:FY1-PROF;
function postF(sx,sy,z0,z1){ return box(pxs(sx),pxs(sx)+PROF,pys(sy),pys(sy)+PROF,z0,z1,...ALU); }
function railX(sy,z){ return box(FX0,FX1,pys(sy),pys(sy)+PROF,z-PROF,z,...ALU); }            // 좌우 : 전장 관통
function railY(sx,z){ return box(pxs(sx),pxs(sx)+PROF,FY0+PROF,FY1-PROF,z-PROF,z,...ALU); }  // 깊이 : 사이 맞물림
```
- 전면 가로대는 **시야를 가리므로 최소화** (하부·최상단만 유지)

### 4-4. 축측투영 가림(painter) 문제 해결
단일 도시 순서로는 해결 안 되는 겹침이 발생. 아래 2가지 기법 사용.
- **부분 재도시 + 클리핑**: 3D 박스 8꼭짓점을 투영해 볼록껍질(convex hull)로 `clipPath` 생성 → 해당 구간만 위에 다시 그림
```javascript
function boxClip(id,x0,x1,y0,y1,z0,z1){
  const ps=[]; [x0,x1].forEach(X=>[y0,y1].forEach(Y=>[z0,z1].forEach(Z=>ps.push(P(X,Y,Z)))));
  return `<clipPath id="${id}"><polygon points="${hull(ps).map(pt).join(' ')}"/></clipPath>`;
}
```
- **프레임 재도시**: 클립 재도시 후 앞기둥을 한 번 더 그려 순서 복원 (안 하면 컨베이어가 프레임 앞으로 튀어나와 보임)
- **degenerate bbox 주의**: 정면/측면에서 선(line)의 bbox가 0이 되면 `filter`·`objectBoundingBox` 그라데이션이 무효화되어 **렌더링 자체가 안 됨** → 3D 박스 폴리곤으로 작도할 것

### 4-5. 라벨/치수 가독성
- 기체 위에 겹치는 텍스트는 **흰색 헤일로** 적용
  `paint-order="stroke" stroke="#FFFFFF" stroke-width="3" stroke-linejoin="round"`
- 축측투영에서는 "바깥"이 x좌표만으로 정해지지 않음. 치수선은 앞(-Y) 또는 뒤(+Y)로 밀어 실루엣을 피할 것

### 렌더링 (실사풍)
- SVG `<defs>` 그라데이션: 알루미늄/스틸/벨트/케이스/기판/돔조명/바닥
- 필터: 드롭섀도우, 블러(접지 그림자), 글로우(조명·알람·스캔)
- PCB: 검은 케이스 + 알루미늄 커버 + 초록 기판(회로 패턴) + 케이블 + 오렌지 커넥터

### 공통 기능
- 자동/수동 신호 전환, 속도 슬라이더, 판정 데모 선택(자동/OK/NG)
- MES 전송 표시, 경광등, 카운터(완료/OK/NG), 동작 로그
- **모바일**: 애니메이션 카드 상단 고정(sticky), 플로우는 현재 단계 1줄만 표시
- **캐시 방지 + 자동 갱신**: no-cache 메타 + 30초마다 서버 파일 변경 감지 → 새 버전 자동 반영

---

## 5. 이어서 할 작업

### 완료 (2026-08-26)
- [x] `vision-jig.html` 안착지그 순환식 신규 작성 → **v1.4** (지그 2대 파이프라인·간섭 회피)
- [x] `jig2.html` 지그 7EA 벨트 고정 + 작업대 매립 신규 작성 → **v1.1**
- [x] `frame.html` 실물 검사기 신규 작성 → **v1.8** (프레임·커버·양문도어·모서리 맞물림)

### 완료 (2026-08-24)
- [x] `Vision-H1.html` 컨베이어 양끝 라운드 처리 → v1.2
- [x] 동일 처리를 `vision3.html`(v3.5), `vision.html`(v1.7)에 적용해 3종 통일
- [x] `vision3.html` 축이 기둥 안쪽에서 이동 + X축 레일 단축 → v3.6 → **v3.7**(앞 기둥 2주 삭제)
- [x] `Vision-H1.html` 헥사포드 스트럿 가동 가시화 + 빔 하단 부착 → **v1.3**

### 미착수 (후보)
- [ ] `frame.html` 후면 배선·에어 배관, 컨베이어 구동 모터 표현
- [ ] `pnp_flow.html` 컨베이어에도 라운드 마감 적용 (현재 미적용, 4-1 코드 이식)
- [ ] classic PAT → fine-grained PAT 교체 (`Insys-review` 단일 범위)
- [ ] 3종 시뮬레이터 데이터의 Firebase 연동 (검사 카운터·판정 이력)

---

## 6. 검증 방법 (Claude 작업 시)

```bash
# JS 문법 검사
python3 -c "
import re
html=open('/mnt/user-data/outputs/파일명').read()
ms=re.findall(r'<script>(.*?)</script>',html,re.S)
open('/tmp/x.js','w').write('\n;\n'.join(ms))
" && node --check /tmp/x.js

# 헤드리스 렌더 확인 (puppeteer-core, 작업 폴더에서 npm install puppeteer-core)
# chrome: /home/claude/.cache/puppeteer/chrome/linux-131.0.6778.204/chrome-linux64/chrome
```
- 렌더 후 SVG 요소 bbox로 **잘림 여부** 확인 (정면/45°/측면 3시점)
- `pageerror` 리스너로 런타임 오류 0건 확인
- `#btnRun` 클릭 후 15초 구동 → 로그 텍스트로 **1사이클 완주** 확인
- 축 값·초점거리 표시가 정상인지 DOM 텍스트로 확인
